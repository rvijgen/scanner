WIFI Scanner

OSX
run: sudo node scan

=======================================================================================
Outputs per channel package count by default, uncomment line 26 for raw tshark output.

Requires T-Shark:
brew install wireshark
